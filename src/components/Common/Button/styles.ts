import { StyleSheet } from 'react-native';

export const buttonStyles = StyleSheet.create({
  button: {
    alignItems: 'center',
    borderRadius: 10,
    color: '#FFF',
    height: 44,
    justifyContent: 'center',
    paddingBottom: 10,
    paddingLeft: 15,
    paddingRight: 15,
    paddingTop: 10,
  },
  primary: {
    backgroundColor: '#4A84EE',
  },
});

export const textStyles = StyleSheet.create({
  primary: {
    color: '#FFF',
    fontSize: 17,
    letterSpacing: -0.41,
    lineHeight: 22,
  },
});
