import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  engine: {
    flex: 1,
  },
  start: {
    backgroundColor: 'blue',
    flex: 1,
    color: 'red',
    maxHeight: 40,
    width: '100%',
    zIndex: 20,
  },
});
