import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  engine: {
    flex: 1,
  },
  loader: {
    alignItems: 'center',
    backgroundColor: '#FFF',
    bottom: 0,
    flex: 1,
    justifyContent: 'center',
    left: 0,
    position: 'absolute',
    right: 0,
    top: 0,
    zIndex: 21,
  },
});
